---
import Button from "@components/ui/Button.astro";
import { Github, Linkedin, Mail } from "@lucide/astro";
import heroBg from "@/assets/hero-bg.webp";
---

<section
    class="relative min-h-screen flex items-center justify-center overflow-hidden bg-background text-foreground"
>
    
    <div class="absolute inset-0 z-0">
        <img
            src={heroBg.src}
            alt="Hero background"
            class="w-full h-full object-cover object-start opacity-20"
        />
    </div>

    
    <div
        class="absolute inset-0 z-10 bg-gradient-to-br from-background/80 via-background/70 to-primary/30 backdrop-blur-sm"
        
        >
    </div>

    
    <div
        class="relative container mx-auto px-6 z-20 text-center animate-in fade-in slide-in-from-bottom-4 duration-1000"
    >
        <span class="inline-block px-4 py-2 rounded-full bg-[hsl(var(--primary)/0.15)] text-[hsl(var(--accent))] text-sm font-medium mb-6 animate-in fade-in slide-in-from-top-4 duration-700 delay-150">
            Junior Cloud Engineer
        </span>
        <h1
            class="text-5xl md:text-7xl font-bold mb-6 bg-gradient-to-r from-[hsl(var(--foreground))] via-[hsl(var(--foreground)/0.8)] to-[hsl(var(--primary))] bg-clip-text text-transparent"
        >
            Jesús Merlo
        </h1>

        <p
            class="text-xl md:text-2xl text-muted-[hsl(var(--foreground))] mb-2 max-w-2xl mx-auto"
        >
            Contruyendo castillos en las nubes. Integración y entrega continua. Pero sobre todo, aprendizaje continuo.
            
        </p>
        <span
            class="text-l md:text-xl text-muted-[hsl(var(--foreground))] mb-10 max-w-2xl mx-auto"
        >
            <i>"La curiosidad es el apetito del conocimiento."</i>
        </span>

        <div class="flex flex-wrap gap-4 justify-center my-12">
            <Button
                variant="accent"
                size="lg"
                class="gap-2 shadow-lg hover:shadow-xl transition-shadow"
                onClick="window.location.href='#contact'"
            >
                <Mail class="h-5 w-5" />
                Contáctame
            </Button>
        </div>

        <div class="flex gap-6 justify-center">
            <a
                href="https://github.com/jesuslabtech"
                target="_blank"
                rel="noopener noreferrer"
                class="text-muted-[hsl(var(--foreground))] hover:text-[hsl(var(--primary))] transition-colors"
            >
                <Github class="h-6 w-6" />
            </a>

            <a
                href="https://linkedin.com/in/jesusmerlo"
                target="_blank"
                rel="noopener noreferrer"
                class="text-muted-[hsl(var(--foreground))] hover:text-[hsl(var(--primary))] transition-colors"
            >
                <Linkedin class="h-6 w-6" />
            </a>
        </div>
    </div>
</section>
